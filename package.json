{
  "name": "househunt-root",
  "version": "1.0.0",
  "scripts": {
    "backend": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf dist && mv frontend/dist dist",
    "start": "npx concurrently \"node backend/server.js\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "concurrently": "^9.2.1",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.5",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.13"
  }
}
